name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.21'
      - name: Build and test backend
        run: |
          cd backend
          go mod tidy
          go test ./...
          go build -o texas-backend ./
      - name: Build and push backend image
        uses: docker/build-push-action@v4
        with:
          context: backend
          push: true
          tags: gcr.io/${{ secrets.GCP_PROJECT }}/texas-backend:latest
          platforms: linux/amd64
  build-frontend:
    runs-on: ubuntu-latest
    needs: build-backend
    steps:
      - uses: actions/checkout@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      - name: Build and push frontend image
        uses: docker/build-push-action@v4
        with:
          context: frontend
          push: true
          tags: gcr.io/${{ secrets.GCP_PROJECT }}/texas-frontend:latest
          platforms: linux/amd64
